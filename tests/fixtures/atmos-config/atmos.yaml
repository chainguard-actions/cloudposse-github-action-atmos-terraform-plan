base_path: "../../.."
components:
  terraform:
    base_path: "tests/fixtures/components/terraform"
    apply_auto_approve: false
    deploy_run_init: true
    init_run_reconfigure: true
    auto_generate_backend_file: false
stacks:
  base_path: "tests/fixtures/stacks"
  included_paths:
    - "**/*"
  excluded_paths: []
  name_pattern: "{stage}"
logs:
  level: Info
